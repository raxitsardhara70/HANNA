{
  "name": "hanna",
  "version": "0.1.0",
  "private": true,
  "description": "Production-grade AI desktop assistant.",
  "author": "HANNA Contributors",
  "type": "module",
  "main": "apps/desktop/dist/main/index.js",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build --workspace=@hanna/desktop",
    "build:packages": "tsc -b packages/types packages/utils packages/shared packages/logger packages/config packages/ipc packages/ui",
    "dev": "node scripts/dev.mjs",
    "dev:desktop": "tsc -b apps/desktop --watch --preserveWatchOutput",
    "dev:electron": "node scripts/dev.mjs --electron-only",
    "dev:packages": "tsc -b packages/types packages/utils packages/shared packages/logger packages/config packages/ipc packages/ui --watch --preserveWatchOutput",
    "dev:renderer": "vite --config apps/desktop/vite.config.mjs --configLoader native --host 127.0.0.1 --port 5173 --strictPort",
    "dist": "npm run build && electron-builder --config electron-builder.yml --dir",
    "lint": "eslint .",
    "format": "prettier --write .",
    "typecheck": "npm run build:packages && tsc -b apps/desktop --pretty false",
    "clean": "node scripts/clean.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.31.0",
    "@types/node": "^24.13.3",
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.6.0",
    "electron": "^43.1.1",
    "electron-builder": "^26.0.12",
    "eslint": "^9.31.0",
    "eslint-config-prettier": "^10.1.5",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.20",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.37.0",
    "vite": "^7.0.5"
  }
}
